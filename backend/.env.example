DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/budgeting

# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=change_me

# Generate: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Plaid credentials (https://dashboard.plaid.com)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Anthropic key for AI categorization
ANTHROPIC_API_KEY=

# Comma-separated list of allowed CORS origins (production)
# Example: https://clover.vercel.app,https://www.clover.app
ALLOWED_ORIGINS=
